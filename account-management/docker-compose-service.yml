version: "2"
#
# Services
#
services:
   account-view-service:
      build: ./account-view-service/
      volumes:
        - ./docker/account-view-service/config:/config
      ports:
        - 8082:8082
      networks:
          - localnet


# Networks
#
networks:
  localnet:
    # driver: bridge
    external: true